services:
  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD:-disposight_dev}
    expose:
      - "6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
